{
  "name": "order-bot",
  "version": "1.0.0",
  "description": "Order items through Slack",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "eb init -p node.js && eb create dev-env",
    "deploy": "eb deploy -r us-east-1",
    "test": "jest"
  },
  "author": "jasonrundell",
  "license": "MIT",
  "devDependencies": {
    "jest": "^23.6.0"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "slackbots": "^1.2.0"
  }
}
